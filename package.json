{
  "name": "mod-validator",
  "version": "1.0.0",
  "description": "Validator for game mod files",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "mod-validator": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "build:bundle": "tsc && node build-bundle.js",
    "build:cli": "tsc",
    "watch": "tsc --watch",
    "test": "node dist/test.js"
  },
  "keywords": [
    "mod",
    "validator",
    "game"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "commander": "^14.0.2"
  }
}
